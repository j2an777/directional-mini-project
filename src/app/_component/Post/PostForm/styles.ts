'use client';

import styled from '@emotion/styled';

const FormWrapper = styled.form`
  width: 100%;
  max-width: 400px;
  height: 100%;
  display: flex;
  flex-direction: column;
  gap: 12px;
`;

const Block = styled.div`
  display: flex;
  flex-direction: column;
  gap: 12px;
`;

export { FormWrapper, Block };
